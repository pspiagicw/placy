import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ReactToPost = ({ navigation}) => {
    return (
        <View>
            <Text>CommentOrReply</Text>
        </View>
    )
}

export default ReactToPost

const styles = StyleSheet.create({})